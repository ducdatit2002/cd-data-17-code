START
  ↓
Xác định tên file Excel
  ↓
File có tồn tại?
  ↓
┌───────────────┬────────────────┐
│ Có            │ Không          │
│               │                │
│ Mở file cũ    │ Tạo file mới   │
│               │                │
│ Lấy worksheet │ Tạo worksheet  │
│               │ Tạo header     │
└───────────────┴────────────────┘
            ↓
      Nhập người dùng
            ↓
        Nhập họ tên
            ↓
       Nhập ngày sinh
            ↓
         Nhập email
            ↓
      Email hợp lệ?
       ↙         ↘
    Không         Có
      ↓            ↓
  Nhập lại      Đi tiếp
                   ↓
              Nhập phone
                   ↓
             Phone hợp lệ?
              ↙        ↘
           Không        Có
             ↓           ↓
          Nhập lại    Đi tiếp
                         ↓
                   Nhập công việc
                         ↓
                   Tổng hợp dữ liệu
                         ↓
                  Append vào Excel
                         ↓
                     Save file
                         ↓
                   Muốn nhập tiếp?
                     ↙        ↘
                   Có          Không
                    ↓             ↓
               Quay lại        STOP

# Một số lỗi thường gặp:
1. Lỗi chưa cài openpyxl

ModuleNotFoundError:
No module named 'openpyxl'

2.Lỗi cài package sai môi trường python

Đã pip install openpyxl -> nhưng ko chạy được 
-> python -m pip install openpyxl

3. Lỗi viết sai Workbook -> workbook

4. Lỗi Gọi phương thức nhưng ko có ()
openpyxl.Workbook -> Lỗi

5. Lỗi viết active() 
worksheet = workbook.active -> Thiếu () 

6. Lỗi quên lưu Workbook 
workbook.save(excel_file)

7. Đang mở file -> ko thấy được dữ liệu

Tư duy CRUD
- Create 
- Read
- Update
- Delete 